{{!-- MCP tool instructions for GitHub operations --}}
I'll use the GitHub MCP server tools to create the issue:

1. **github_create_issue** - Create the issue with:
   - Repository: {{github.repository}}
   - Title: [Based on our discussion]
   - Body: [Complete task description we developed]
   {{#if github.defaultLabels}}- Labels: {{#each github.defaultLabels}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} + any additional relevant labels{{/if}}

2. **github_list_issues** - Verify the issue was created successfully

The MCP server handles authentication and repository access automatically.